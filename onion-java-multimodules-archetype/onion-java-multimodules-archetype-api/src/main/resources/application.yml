spring:
  jpa:
    hibernate:
      ddl-auto: none
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/onboarding
    username: onboarding
    password: p4sswordonboarding

server:
  port: 8080